{% extends 'base.html.twig' %} {% block title %} Homepage {% endblock %} {%
block body %}

<div class="min-h-screen w-full bg-[#020617] relative">
    <div
        class="absolute inset-0 z-0"
        style="
            background-image: radial-gradient(
                circle 500px at 50% 100px,
                rgba(139, 92, 246, 0.4),
                transparent
            );
        "
    ></div>
    <div class="z-10">
        {{
            component(
                "Hero",
                {
                    title: "Your board, your power !",
                    subtitle:
                        "Organize your ideas, manage your projects, move forward without limits.",
                    size: "full"
                },
                { class: "mb-6 rounded-2xl overflow-hidden" }
            )
        }}

        {# Features Section #} {% include 'home/_features.html.twig' %} {# CTA
        Section #} {% include 'home/_cta.html.twig' %}
    </div>
</div>
{% endblock %}
